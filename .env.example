# Optional. The embedded data snapshot in data/embedded/ is used unless both are set.
# FLOWLENS_DATA_SOURCE=postgres
# DATABASE_URL=postgresql://postgres:[password]@[host]:5432/postgres
# DATABASE_SSL=true
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Inputs for `pnpm build:embedded-data` / `pnpm db:import-cleaned` (kept out of git).
FLOWLENS_CLEANED_DATA_DIR=./cleaned_data6
FLOWLENS_CSV_PATH=./OECD Dataset.xlsx - complete_p4d3_df.csv
